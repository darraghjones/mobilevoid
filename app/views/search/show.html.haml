%h1 Search#show

=render "form"

%ul
  -@songs.each do |s|
    %li
      %a{:href => "http://www.legalsounds.com#{s.Url}"}=s.Name
      %a{:href => s.Artist.Url}=s.Artist.Name
      %a{:href => s.Album.Url}=s.Album.Name

